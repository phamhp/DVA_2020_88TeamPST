It is where the files hosted on Heroku cloud so people can visit the website everywhere online.
To experiment the webserver locally. 
- Install:
++ virtualenv for python.
++ pip install -r requirement

- Test locally before push to git by:
++ Run cmd: flask run 
++ Open browser, enter : localhost:5000

